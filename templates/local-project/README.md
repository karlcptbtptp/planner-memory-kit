# Local Project Template

把本目录里的结构复制到项目根目录，用于落地三模块：

- `03_memory/`：智能体记忆
- `90_tools/personal/`：个人工具
- `02_outputs/`：项目产出

复制后：

1. 合并 `AGENTS.memory.template.md` 到项目 `AGENTS.md`。
2. 合并 `gitignore.snippet` 到项目 `.gitignore`。
3. 配置 `memory:sync:feishu` 命令。
4. 执行 dry-run 验证项目隔离。

